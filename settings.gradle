pluginManagement {
    includeBuild 'build-logic'
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'omni-mart'

include ':api-gateway'
include ':service-discovery'
include ':config-server'

include ':shared:jpa-support'
